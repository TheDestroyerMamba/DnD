#ifndef ALIGNEMENT_HPP
#define ALIGNEMENT_HPP

#include <string>
#include <stdint.h>

using namespace std;

class Alignement {
	private:
		

	public:
		Alignement(void);
		Alignement(Alignement* alignement);
		Alignement();
		
		
};

#endif
